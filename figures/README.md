# Figures

README figures are generated from the current analytical pipeline. During Phase 1, committed images use the deterministic synthetic development fixture and are labeled accordingly. Replace those snapshots after the first authenticated EIA pull and preserve the interpretation beneath each visual.
